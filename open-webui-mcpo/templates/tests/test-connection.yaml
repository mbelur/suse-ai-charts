apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-webui-mcpo.fullname" . }}-test-connection"
  labels:
    {{- include "open-webui-mcpo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['http://{{ include "open-webui-mcpo.fullname" . }}:{{ .Values.service.port }}/openapi.json']
